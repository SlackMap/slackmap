language: node_js
node_js:
  - 8
script:
  - npm run build api
  - ls -la ./dist/apps/api
after_success :
  - npm run version:test
  - echo 'on dev only'
  on:
    branch: development
after_script:
  - npm run travis
  - echo 'on master only'
  on:
    branch: master
